\* Expected: VIOLATED (the StoreLoad window is open under StoreStore relaxation
\* too — every TSO behavior is also an "arm" behavior).
SPECIFICATION Spec
INVARIANT NoUseAfterFree
CONSTANT Model = "arm"
