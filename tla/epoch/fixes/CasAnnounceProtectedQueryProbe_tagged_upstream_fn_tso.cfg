\* Expected: VIOLATED. control (order axis): upstream Release() order also breaks the query the other way on plain x86
\* x86-TSO: the store buffer drains in FIFO order, only StoreLoad is relaxed.
SPECIFICATION Spec
INVARIANT NoFalseNegative
CONSTANT Model = "tso"
CONSTANT QueryKind = "tagged"
CONSTANT ReleaseOrder = "upstream"