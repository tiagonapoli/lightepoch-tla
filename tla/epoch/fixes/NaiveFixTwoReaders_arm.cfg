\* Expected: VIOLATED. Release()'s two plain stores are unordered, so the
\* threadId clear can be seen first and the epoch clear can land on the NEXT
\* owner's announce.
SPECIFICATION Spec
INVARIANT NoUseAfterFree
INVARIANT SlotExclusive
INVARIANT AnnounceNotWiped
CONSTANT Model = "arm"
