\* Expected: HOLDS. control (fence axis): FIFO drain already orders the clear before the unpublish
\* x86-TSO: the store buffer drains in FIFO order, only StoreLoad is relaxed.
SPECIFICATION Spec
INVARIANT NoUseAfterFree
INVARIANT SlotExclusive
INVARIANT ProtectedQueryAccurate
INVARIANT NoLostSuspend
INVARIANT NoDoubleAcquire
CONSTANT Model = "tso"
CONSTANT ReleaseOrder = "plain"
