\* CONTROL, expected to FAIL. Same symmetric-peer scenario, but Acquire uses
\* production's ordering: CAS on threadId, then a PLAIN announce of the epoch.
\* Confirms the spec is actually capable of detecting the bug, so that the
\* HOLDS result for CasAnnounceSymmetricPeers is meaningful.
SPECIFICATION Spec
INVARIANT NoUseAfterFree
INVARIANT AnnounceVisibleWhileProtected
CONSTANT Model = "tso"
