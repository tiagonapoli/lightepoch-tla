\* Expected: VIOLATED. Live control for CasAnnounceTwoReadersNoThreadId: the
\* claim is a plain buffered store instead of an atomic RMW, so two readers
\* both claim the one slot and the reclaimer frees under a live reader.
SPECIFICATION Spec
INVARIANT NoUseAfterFree
INVARIANT SlotExclusive
CONSTANT Model = "arm"
