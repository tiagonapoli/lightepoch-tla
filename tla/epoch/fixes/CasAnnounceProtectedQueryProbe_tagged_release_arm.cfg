\* Expected: HOLDS. the fix on a contended two-slot table: accurate in both directions (+StoreStore)
\* Store-order relaxation of TSO: any pending store may become visible next,
\* so a thread's stores can be observed out of program order.
SPECIFICATION Spec
INVARIANT NoFalsePositive
INVARIANT NoFalseNegative
INVARIANT OwnedImpliesAnnounced
CONSTANT Model = "arm"
CONSTANT QueryKind = "tagged"
CONSTANT ReleaseOrder = "release"