\* Expected: VIOLATED. control (order axis): upstream Release() publishes the slot free while the tag still matches -- a false POSITIVE, on plain x86
\* x86-TSO: the store buffer drains in FIFO order, only StoreLoad is relaxed.
SPECIFICATION Spec
INVARIANT NoFalsePositive
CONSTANT Model = "tso"
CONSTANT QueryKind = "tagged"
CONSTANT ReleaseOrder = "upstream"