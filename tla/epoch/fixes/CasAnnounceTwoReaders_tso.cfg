\* Expected: HOLDS. Two readers contending for one slot, plus a reclaimer.
\* x86-TSO store buffer: FIFO drain, only StoreLoad is relaxed.
SPECIFICATION Spec
INVARIANT NoUseAfterFree
INVARIANT SlotExclusive
INVARIANT ThreadIdIntact
CONSTANT Model = "tso"
