\* Expected: VIOLATED. control (tag axis): drop threadId and the probe window reports protection the thread does not have
\* x86-TSO: the store buffer drains in FIFO order, only StoreLoad is relaxed.
SPECIFICATION Spec
INVARIANT NoFalsePositive
CONSTANT Model = "tso"
CONSTANT QueryKind = "indexonly"
CONSTANT ReleaseOrder = "release"