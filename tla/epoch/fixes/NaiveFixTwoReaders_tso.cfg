\* Expected: HOLDS. FIFO drain forces the epoch clear out before the threadId
\* clear, so a claimant that sees threadId == 0 also sees the epoch cleared.
SPECIFICATION Spec
INVARIANT NoUseAfterFree
INVARIANT SlotExclusive
INVARIANT AnnounceNotWiped
CONSTANT Model = "tso"
