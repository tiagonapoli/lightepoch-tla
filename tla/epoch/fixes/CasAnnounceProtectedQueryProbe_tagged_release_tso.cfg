\* Expected: HOLDS. the fix on a contended two-slot table: the query is accurate in both directions (x86-TSO)
\* x86-TSO: the store buffer drains in FIFO order, only StoreLoad is relaxed.
SPECIFICATION Spec
INVARIANT NoFalsePositive
INVARIANT NoFalseNegative
INVARIANT OwnedImpliesAnnounced
CONSTANT Model = "tso"
CONSTANT QueryKind = "tagged"
CONSTANT ReleaseOrder = "release"