\* Expected: HOLDS — the fix survives StoreStore relaxation as well, so it does
\* not depend on the FIFO drain order that TSO happens to provide.
SPECIFICATION Spec
INVARIANT NoUseAfterFree
CONSTANT Model = "arm"
