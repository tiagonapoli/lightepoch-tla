\* Expected: VIOLATED. the bug: the plain unpublish is observed while the dereference is still in flight
\* The algorithm is the CAS-carried announce in every row; only the
\* ordering strength of the Release store varies.
SPECIFICATION Spec
INVARIANT NoUseAfterFreeDereference
INVARIANT NoFreeWhileDereferenceInFlight
CONSTANT Model = "arm"
CONSTANT ReleaseOrder = "plain"
