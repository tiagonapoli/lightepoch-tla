\* Expected: HOLDS. One reader + one reclaimer, full Tsavorite sequence
\* (Acquire -> ProtectAndDrain -> critical section -> Suspend).
\* Total Store Order: a thread's stores become visible in program order.
SPECIFICATION Spec
INVARIANT NoUseAfterFree
INVARIANT AnnounceVisibleWhileProtected
CONSTANT Model = "tso"
