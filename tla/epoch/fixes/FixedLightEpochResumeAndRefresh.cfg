\* Expected: HOLDS (fencing both announce sites — Acquire and ProtectAndDrain —
\* closes the window for Tsavorite's per-op Resume+Refresh+Suspend sequence).
SPECIFICATION Spec
INVARIANT NoUseAfterFree
