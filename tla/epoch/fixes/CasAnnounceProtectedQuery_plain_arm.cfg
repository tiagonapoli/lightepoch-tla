\* Expected: VIOLATED. control (fence axis): the unpublish overtakes the clear and the next owner's tag is wiped
\* Store-order relaxation of TSO: any pending store may become visible next,
\* so a thread's stores can be observed out of program order.
SPECIFICATION Spec
INVARIANT NoUseAfterFree
INVARIANT SlotExclusive
INVARIANT ProtectedQueryAccurate
INVARIANT NoLostSuspend
INVARIANT NoDoubleAcquire
CONSTANT Model = "arm"
CONSTANT ReleaseOrder = "plain"
