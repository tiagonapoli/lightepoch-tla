\* Expected: VIOLATED. Live control (TSO) for CasAnnounceTwoReadersNoThreadId:
\* the claim is a plain buffered store instead of an atomic RMW, so two
\* readers both claim the one slot.
SPECIFICATION Spec
INVARIANT NoUseAfterFree
INVARIANT SlotExclusive
CONSTANT Model = "tso"
