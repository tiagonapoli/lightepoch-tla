\* Expected: HOLDS (Acquire announces + fences; the immediately-following
\* ProtectAndDrainWithoutAnnounce() skips the redundant second announce/fence,
\* leaving lce pinned at the epoch Acquire published — still safe).
SPECIFICATION Spec
INVARIANT NoUseAfterFree
