\* Expected: HOLDS (a full StoreLoad barrier after the announce closes it).
\* x86-TSO store buffer: FIFO drain, only StoreLoad is relaxed.
SPECIFICATION Spec
INVARIANT NoUseAfterFree
CONSTANT Model = "tso"
